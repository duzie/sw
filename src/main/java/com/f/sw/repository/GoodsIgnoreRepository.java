package com.f.sw.repository;

import com.f.sw.entity.GoodsIgnore;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GoodsIgnoreRepository extends JpaRepository<GoodsIgnore, String> {
}
